# ASDF Command-Line cheatsheet

## References
# Docs
https://asdf-vm.com

# ASDF Git repository
https://github.com/asdf-vm/asdf

# ASDF plugin repository
https://github.com/asdf-vm/asdf-plugins

# Show version
asdf --version

## Plugins
# List plugins registered on asdf-plugins repository with URLs
asdf plugin list all

# Add a plugin from the plugin repo
asdf plugin add <name> [<git-url>]

# Remove plugin and package versions
asdf plugin remove <name>

# Update all plugins
asdf plugin update --all

## Manage
# Install 
asdf install <name>
asdf install <name> <version>
asdf install <name> latest[:<version>]

# Uninstall
asdf uninstall <name> <version>

# Set Module version
asdf local <name> <version>
asdf global <name> <version>
asdf shell <name> <version>

# Show Current
asdf current <name>

# List Installed
asdf list <name>
asdf list all <name> [<version>]

# Help
asdf help <name> [<version>]
